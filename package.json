{
  "name": "swifty-server-heroku",
  "version": "1.0.0",
  "description": "",
  "engines": {
    "node": "9.x"
  },
  "scripts": {
    "start": "cd server/ && npm start",
    "heroku-prebuild": "cd server/ && npm install"
  }
}
